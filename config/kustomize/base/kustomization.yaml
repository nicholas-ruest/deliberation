apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The manifests in config/kubernetes are the canonical, policy-checked source
# (scripts/check-deployment.ts validates them directly). They are referenced,
# never copied. This base applies no transformations of its own: every
# environment-specific value belongs in an overlay.
resources:
  - ../../kubernetes
